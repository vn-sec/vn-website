---
import Layout, { type Props as LayoutProps } from "@/layouts/Layout.astro";
import Window from "@/components/helpers/WindowLayoutPage.astro";

export interface Props extends LayoutProps {}

const { ...layoutProps } = Astro.props;
---

<Layout {...layoutProps}>
    <Window><slot /></Window>
</Layout>
